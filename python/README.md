# 01 - Python
A folder containing Python problems.

## Files

### `python-1.ipynb`
Python basics: arithmetic operations (with user input via `input()`), circle/rectangle calculations using the `math` module, string operations (indexing, slicing, escape characters), and type checking with `type()`.

---

**Tools used:** Python (Jupyter Notebook), `math` module